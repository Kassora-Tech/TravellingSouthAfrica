# Preview Project Workflow

This workflow starts the development preview server for the NextJS application.

1. **Start Development Server**:
   ```bash
   // turbo
   npm run dev -- --port $PORT --hostname 0.0.0.0
   ```
   *(Note: You can use just `npm run dev` for default local execution)*
